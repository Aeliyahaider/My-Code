#include <iostream>
#include <vector>
using namespace std;

int main(){
	
	vector<int> twoSum(4), (vector<int>& arr[],target);
	int n = sizeof(arr)/sizeof(arr[0]);
	vector<int> v[2];
	for(int i=0; i<n; i++){	
			if(arr[i]==arr[k]){
				v[0]=i;
				v[1]=k;
				
		}
	}	
}